/** Shared header logo sizing so Main / Marketing / Talent / Technology match. */
export const HEADER_LOGO_WIDTH = 160;
export const HEADER_LOGO_HEIGHT = 32;
/** Fixed 32px height — Main / Marketing / Talent logos fill this cleanly. */
export const HEADER_LOGO_CLASS =
  "h-8 w-auto max-h-8 object-contain object-left select-none";

/**
 * Technology SVG has extra canvas padding, so it needs a slightly taller
 * display size to match the Main homepage logo optically.
 */
export const HEADER_LOGO_TECH_WIDTH = 200;
export const HEADER_LOGO_TECH_HEIGHT = 40;
export const HEADER_LOGO_TECH_CLASS =
  "h-10 w-auto max-h-10 object-contain object-left select-none";
